{
  "name": "bui",
  "version": "0.0.0",
  "private": true,
  "description": "BUI: a creation & research forge.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint": "prettier --check .",
    "format": "prettier --write .",
    "test": "node --test 'test/**/*.test.js'"
  },
  "devDependencies": {
    "prettier": "3.3.3"
  }
}
